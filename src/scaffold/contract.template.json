{
  "version": 1,
  "name": "TODO: your app's name",

  "systemVariants": {
    "default": "TODO: paste the EXACT system prompt your app sends in production. Not a summary, not a cleaned-up version -- the string the model actually receives, after every template has been interpolated. See AGENTS.md."
  },

  "tools": [],

  "sampling": {
    "temperature": 0.7,
    "max_tokens": 1024
  },

  "toolChoice": "auto",

  "envelopes": {},

  "reasoning": {
    "inlineTags": [["<think>", "</think>"]],
    "leakPatterns": ["</?think>", "</?reasoning>"]
  },

  "primeUserMessage": "ok"
}
